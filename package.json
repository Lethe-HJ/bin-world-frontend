{
  "name": "bin-world",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"cd frontend && yarn dev\" \"tauri dev\"",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "@tauri-apps/cli": "^2",
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
